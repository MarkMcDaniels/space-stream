import React from 'react';

const navigation = (props) => {

    const items = <p>items</p>;
    return (
        <React.Fragment>
            <ul>
                {items}
            </ul>
        </React.Fragment>
    );
}

export default navigation;